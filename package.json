{
  "name": "circleci-for-jira",
  "version": "1.0.0",
  "description": "Connect your CircleCI Pipelines to Jira issues",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces"
  }
}
